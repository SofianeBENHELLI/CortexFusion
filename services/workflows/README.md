# services/workflows

Python workflows for ingestion, extraction orchestration, summary validation, consolidation, scheduling, and reports. Temporal is the imported design baseline. Workflow workers must use core contracts rather than writing trusted state directly.

Status: planned component; this directory currently documents its responsibility only.
